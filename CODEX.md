# Codex Project Notes (superseded - pointer only)

The Altitude Protocol that used to live here moved on 2026-07-05:

- Global operating protocol: `~/.codex/AGENTS.md` (auto-loaded every session;
  do not re-read here).
- Delegation doctrine, Maintenance rules, and the failure example bank:
  `~/.codex/CODEX.md` (read at the trigger points the global protocol names).
- Repo-specific rules for this repo (text encoding): `AGENTS.md` at this
  repo's root.

Older work orders and construction guides list this file as required reading;
that requirement is satisfied by the files above. Do not add new rules here.
