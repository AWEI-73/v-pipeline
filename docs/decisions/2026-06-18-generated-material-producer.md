# GMP1 Generated Material Producer

Date: 2026-06-18

## Decision

Add a generated-material producer layer after MGF1.

MGF1 plans generated jobs from `material_delta`; GMP1 executes those jobs into
files, manifests, candidate material maps, and a quality review artifact.

## Contract

Input:

- `material_generation_fallback.json`
- `material_needs.json`
- optional `style_profile.json`

Output:

- `generated_images/*.png`
- `generated_asset_requests.json`
- `generated_asset_outputs.json`
- `generated_asset_manifest.json`
- `generated_material_maps/*.map.json`
- `project_material_map.json`
- `generated_material_quality_review.json`
- `generated_material_production.json`

Generated assets always return as `candidate` satisfies edges. They are never
accepted automatically.

## Renderer Boundary

The current `test_pil` renderer is deterministic and offline. It exists to test
the flow, file shape, manifest, material-map linkage, and quality-review logic.

It is not final art.

Real production images should be generated by Gemini, Antigravity,
assistant_imagegen, Codex imagegen, or another provider that writes the same
manifest/output shape.

## Acceptance Evidence

Three small projects were run from no material:

```text
material_needs
  -> material-delta
  -> material-generation-fallback
  -> generated-material-produce
  -> project_material_map
  -> material-delta after generation
```

Artifacts:

- `.tmp/generated_material_flow_cases/case1_training_066`
- `.tmp/generated_material_flow_cases/case2_postcard_comic`
- `.tmp/generated_material_flow_cases/case3_event_bridge`
- `.tmp/generated_material_flow_cases/FLOW_REVIEW.md`

Findings:

- images and material maps are produced;
- generated assets enter as candidate;
- post-generation delta changes from `missing` to `thin`, not `covered`, because
  candidate-only evidence still requires review acceptance;
- this is correct and preserves the M6 truth boundary.

## Verification

Focused:

```text
python -m unittest tests.test_generated_material_producer tests.test_material_generation_fallback tests.test_generated_assets -q
18 tests OK

python -m unittest tests.test_video_tools_command_catalog -q
4 tests OK
```

## Deferred

- Direct API callers for GPT image / Gemini / Antigravity. Current integration
  intentionally starts from completed provider files.
- Strong visual consistency scoring from pixels. Current lock is metadata-based
  style/character anchor validation.
- Human/agent review step that can promote generated candidate evidence to
  accepted.

## GMP2 Provider Output Intake

Date: 2026-06-19

The real-provider boundary is now a file intake contract, not an API caller.
External tools such as GPT image, Gemini, or Antigravity write images and a
provider output JSON. The pipeline imports those files with:

```powershell
python video_tools.py generated-material-import material_generation_fallback.json `
  --needs material_needs.json `
  --provider-outputs generated_provider_outputs.json `
  --style-profile style_profile.json `
  --out-dir generated_materials
```

The import layer:

- requires each provider output to match a planned `job_id`;
- requires at least `panel_count` readable image files per job;
- copies files into `generated_images/`;
- writes the same manifest/map/review artifacts as the offline renderer;
- fails quality if declared style or character anchors are missing;
- still emits only `candidate` satisfies edges.

This keeps external model behavior outside the deterministic backend while
making generated assets usable by the material-map lifecycle.

## GMP3 Skill Acceptance Harness

Date: 2026-06-19

Add a reproducible harness:

```powershell
python tools/generated_material_flow_acceptance.py .tmp/generated_material_skill_acceptance
```

It runs two comic-style projects from no material:

- `case_a_rain_station`
- `case_b_rooftop_postcard`

Each case performs:

```text
material_needs
  -> material_delta missing
  -> material_generation_fallback
  -> generated_material_produce
  -> generated project_material_map
  -> material_delta after generation
  -> director-style score + contact sheet
```

Expected outcome:

- initial delta: `missing=2`
- generated images: `4`
- post-generation delta: `missing=0`, `thin=2`
- director score: `>=80`

The `thin` result is correct. Generated panels are usable material candidates,
but they must be reviewed before promotion to `accepted`.
